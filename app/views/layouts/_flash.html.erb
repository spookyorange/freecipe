<% flash.each do |flash_type, message| %>
  <div data-controller="flash" data-flash-target="itself" class="animate-initiateObjectThenKillOpacity flash flash-<%= "#{flash_type}" %>">
    <%= message %>
  </div>
<% end %>